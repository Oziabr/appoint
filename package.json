{
  "name": "appoint",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "pug src/index.pug --out dist",
    "watch": "pug src/index.pug --out dist --watch"
  },
  "devDependencies": {
    "pug-cli": "*"
  }
}
